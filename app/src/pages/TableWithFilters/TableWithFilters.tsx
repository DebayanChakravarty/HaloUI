import { DataTable, Column, Filter } from '@haloui/ui'; type Row={id:number; name:string; role:string}; const data:Row[]=Array.from({length:12},(_,i)=>({id:i+1,name:`User ${i+1}`,role:i%3?'Editor':'Viewer'})); const cols:Column<Row>[]= [{id:'id',header:'ID',cell:r=>r.id},{id:'name',header:'Name',cell:r=>r.name},{id:'role',header:'Role',cell:r=>r.role}]; const filters:Filter<Row>[]= [{id:'q',label:'Search name',predicate:(row,v)=>!v||row.name.toLowerCase().includes(String(v).toLowerCase())},{id:'role',label:'Role',predicate:(row,v)=>!v||row.role.toLowerCase().includes(String(v).toLowerCase())}]; export default function TableWithFilters(){ return <DataTable columns={cols} data={data} filters={filters} />; }