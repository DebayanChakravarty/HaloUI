import { DataTable, Column } from '@haloui/ui'; type Row={id:number; name:string; role:string}; const data:Row[]=Array.from({length:8},(_,i)=>({id:i+1,name:`User ${i+1}`,role:i%2?'Editor':'Viewer'})); const cols:Column<Row>[]= [{id:'id',header:'ID',cell:r=>r.id},{id:'name',header:'Name',cell:r=>r.name},{id:'role',header:'Role',cell:r=>r.role}]; export default function TableBasic(){ return <DataTable columns={cols} data={data} onRowClick={r=>console.log(r)} />; }